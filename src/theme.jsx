import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1e3a8a', // Blue
    },
    secondary: {
      main: '#000000', // Black
    },
  },
});

export default theme;
